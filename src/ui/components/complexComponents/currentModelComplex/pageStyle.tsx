import { StyleSheet } from "react-native";

export const pageStyle = StyleSheet.create({
  textContainer: {
    alignSelf: "flex-start",
    flexDirection: "row",
    paddingTop: 10,
    gap: 10,
  },
});
